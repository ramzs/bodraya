import Rellax from 'rellax';

export const paralax = () => {
  const rellax = new Rellax('.parallax', {
    speed: 3,
    center: true,
    // wrapper: null,
    // round: true,
    // vertical: true,
    // horizontal: false
  });
}
