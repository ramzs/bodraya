extends ../components/base/layout

block variables
  - const title = "Контакты" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = "wrapper--blue"
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "page-13.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    +section-contacts()

block modals
  +modal("modal--trade")(data-modal="modal-trade")
    +modal-trade()
